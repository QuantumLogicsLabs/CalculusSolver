model:
  hidden_dim: 512
  num_heads: 8
  num_layers: 6
  ffn_dim: 2048
  dropout: 0.1
  max_len: 256

training:
  batch_size: 64
  lr: 2e-4
  warmup_steps: 5000
  max_steps: 50000
  mask_ratio: 0.2
  save_every_steps: 5000
  validate_every_steps: 0
  log_every_steps: 100
